@* 인증 페이지 공통 레이아웃 (좌측 브랜딩 + 우측 폼) *@

<div class="@Css.Layout.PageCenter">
    <div class="@Css.Layout.SplitCard">

        @* 좌측: 브랜딩 패널 *@
        <div class="@Css.Layout.DarkSidePanel">

            @* 장식 원 *@
            <div class="@Css.Layout.DecorCircleTop"></div>
            <div class="@Css.Layout.DecorCircleBottom"></div>

            @* 로고 *@
            <div class="@Css.Layout.PanelLogo">
                Play<span class="text-primary">Ground</span>
            </div>

            @* 브랜드 콘텐츠 (모바일 숨김) *@
            <div class="@Css.Layout.PanelContent">
                @switch (Variant)
                {
                    case AuthVariant.LoginRegister:
                        <h2 class="@Css.Typography.SectionTitle">
                            신뢰할 수 있는 정보로<br>
                            <span class="text-primary">최적의 선택</span>을 돕습니다
                        </h2>
                        <p class="@Css.Typography.BodyOnDark">
                            인증된 팀 정보, 검증된 리뷰, 선수 포트폴리오.<br>
                            데이터 기반으로 올바른 판단을 내릴 수 있는 공간.
                        </p>
                        break;
                    case AuthVariant.Onboarding:
                        <h2 class="@Css.Typography.SectionTitle">
                            맞춤 경험을 위해<br>
                            <span class="text-primary">간단한 정보</span>를 알려주세요
                        </h2>
                        <p class="@Css.Typography.BodyOnDark">
                            입력하신 정보를 바탕으로 관련 팀을 추천하고,<br>
                            맞춤형 대시보드를 준비합니다.
                        </p>
                        break;
                    case AuthVariant.Complete:
                        <h2 class="@Css.Typography.SectionTitle">
                            모든 준비가<br>
                            <span class="text-primary">완료</span>되었습니다
                        </h2>
                        <p class="@Css.Typography.BodyOnDark">
                            지금부터 인증된 팀 정보를 탐색하고,<br>
                            자녀에게 가장 적합한 환경을 찾아보세요.
                        </p>
                        break;
                }

                @* 기능 목록 *@
                <div class="@Css.List.Container">
                    <div class="@Css.List.IconItem">
                        <div class="@(Css.Icon.RoundedBox(Css.Icon.BgNavy))">
                            <svg class="@(Css.Icon.Svg(Css.Icon.StrokeNavy))" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
                        </div>
                        인증된 팀 정보 · 투명한 운영 데이터
                    </div>
                    <div class="@Css.List.IconItem">
                        <div class="@(Css.Icon.RoundedBox(Css.Icon.BgTeal))">
                            <svg class="@(Css.Icon.Svg(Css.Icon.StrokeTeal))" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
                        </div>
                        실명 인증 리뷰 · 신뢰 기반 평가
                    </div>
                    <div class="@Css.List.IconItem">
                        <div class="@(Css.Icon.RoundedBox(Css.Icon.BgOrange))">
                            <svg class="@(Css.Icon.Svg(Css.Icon.StrokeOrange))" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>
                        </div>
                        선수 포트폴리오 · 성장 기록 관리
                    </div>
                    <div class="@Css.List.IconItem">
                        <div class="@(Css.Icon.RoundedBox(Css.Icon.BgViolet))">
                            <svg class="@(Css.Icon.Svg(Css.Icon.StrokeViolet))" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="8.5" cy="7" r="4"/><line x1="20" y1="8" x2="20" y2="14"/><line x1="23" y1="11" x2="17" y2="11"/></svg>
                        </div>
                        검증된 에이전트 · 매칭 지원
                    </div>
                </div>
            </div>

            @* 푸터 *@
            <div class="@Css.Layout.PanelFooter">
                &copy; 2026 PlayGround. All rights reserved.
            </div>
        </div>

        @* 우측: 폼 영역 *@
        <div class="@Css.Layout.ContentPanel">
            @ChildContent
        </div>
    </div>
</div>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public AuthVariant Variant { get; set; } = AuthVariant.LoginRegister;

    public enum AuthVariant
    {
        LoginRegister,
        Onboarding,
        Complete
    }
}
